[scripts.setup-git]
description = "git identity and delta configuration"
file = "scripts/maintain/setup-git.sh"
check = "test -n \"$(git config --global user.email)\" && test \"$(git config --global core.pager)\" = delta && test \"$(git config --global merge.conflictStyle)\" = zdiff3"
