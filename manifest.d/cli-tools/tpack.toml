# Installed from a tap, so the full tap path is required; it ships as a
# cask, so brew-cask mechanics observe it.
[programs.tpack]
description = "tmux package manager"

[programs.tpack.install.brew-cask]
command = "brew install tmuxpack/tpack/tpack"
