# Fedora toolchain batch: the Development Tools group and the build
# libraries used by asdf/pyenv/ruby builds — related, installed together.

[programs.development-tools-group]
description = "dnf Development Tools group"

# A dnf group, not a package: its own install command and check.
[programs.development-tools-group.install.dnf]
command = "sudo dnf group install -y development-tools"
check = "dnf group list --installed 2>/dev/null | grep '^development-tools '"
regex = "(development-tools)"

[programs.gcc]
via = ["dnf"]

[programs.autoconf]
via = ["dnf"]

[programs.patch]
via = ["dnf"]

[programs.rust]
via = ["dnf"]

[programs.findutils]
via = ["dnf"]

[programs.zlib-devel]
via = ["dnf"]

# Virtual provide on modern Fedora: override the dnf installer's check.
[programs.zlib-devel.install.dnf]
check = "rpm -q --whatprovides zlib-devel"

[programs.bzip2-devel]
via = ["dnf"]

[programs.readline-devel]
via = ["dnf"]

[programs.sqlite]
via = ["dnf"]

[programs.sqlite-devel]
via = ["dnf"]

[programs.openssl-devel]
via = ["dnf"]

[programs.xz-devel]
via = ["dnf"]

[programs.libffi-devel]
via = ["dnf"]

[programs.tk-devel]
via = ["dnf"]

[programs.libzstd-devel]
via = ["dnf"]

[programs.libyaml-devel]
via = ["dnf"]

[programs.gdbm-devel]
via = ["dnf"]

[programs.ncurses-devel]
via = ["dnf"]

[programs.dbus-devel]
via = ["dnf"]
description = "D-Bus development headers"

[programs.perl-FindBin]
via = ["dnf"]

[programs.libxcrypt-compat]
via = ["dnf"]

[programs.procps-ng]
via = ["dnf"]
