[programs.konsole]
template = "rpm"
description = "KDE terminal emulator"

[programs.dbus-devel]
template = "rpm"
description = "D-Bus development headers"

# The real package behind the 'dnf-command(copr)' virtual provide — using the
# concrete name keeps it fully template-shaped.
[programs.dnf-plugins-core]
template = "rpm"
description = "dnf plugins (provides the copr command)"
