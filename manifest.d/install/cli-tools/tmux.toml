# brew on macOS; on Omarchy tmux is a base package, mapped to the
# system package manager there so the workmux dependency resolves without brew
# installing a second copy.
[programs.tmux]
via = ["brew", "dnf", "omarchy-pkg"]
